#!groovy

properties([disableConcurrentBuilds()])

pipeline {
    agent {
             label 'master'
             }
        option {
               buildDiscarder(logRotatator(numToKeepStr: '10', artifactNumToKeepStr: '10'))
               timestamps()
     }
   stage {
          stage("Preparations") {
                 step {
                      sh 'ssh root@test1 \'hostname''
                  }
          }
   }
}
